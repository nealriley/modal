# Android Client

Goals:
- Quick audio capture via service/tile/hotkey
- Mode selection UI
- Upload to backend endpoint
- Clipboard write using `ClipboardManager`

Track blockers and implementation notes in `docs/worklog` and `docs/context`.
